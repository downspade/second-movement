# 和時計・旧暦プログラム 実装メモ

## プロジェクト共通情報

- リポジトリ: `second-movement` (joeycastillo/second-movement または voloved/second-movement)
- ボード: Sensor Watch Pro (`BOARD=sensorwatch_pro`)
- 液晶: カスタムLCD、92セグメント (`DISPLAY=custom`)
- ビルドコマンド:
  ```
  make BOARD=sensorwatch_pro DISPLAY=custom
  emmake make BOARD=sensorwatch_pro DISPLAY=custom  # エミュレーター向け
  ```
- 既存の `sunrise_sunset_face.c` / `sunriset.c` (Paul Schlyter版の日の出日の入り計算ライブラリ) を土台に流用する
- 表示API: `watch_display_text`, `watch_display_text_with_fallback`
  - `WATCH_POSITION_TOP` (曜日相当、カスタムLCDで3文字)
  - `WATCH_POSITION_BOTTOM` (時刻相当、6文字程度)
- `movement_faces.h` が全フェイスのヘッダーを一括includeしているため、`movement_config.h` 側で個別に `#include` すると重複定義エラーになることがある。配列に名前を追加するだけでよい。

---

## 1. 和時計 (十二支 + 四分割、薄明基準)

### コンセプト
- 一般的な不定時法(日の出・日の入りで6+6分割)ではなく、**十二支の12区分**を使う。
- 昼夜の境界は「日の出・日の入り」ではなく、**薄明(明六つ・暮六つ)**を基準にする。これにより昼側が実際の日照時間より約1時間長くなる。
- 各十二支の区分(1区分 ≈ 2時間)をさらに**4等分**し、「一つ・二つ・三つ・四つ」で呼ぶ。
- 目標例: 「丑三つ時」(丑の区分の3番目の4分の1、深夜2時台)を正しく表現できること。

### 薄明の定義(重要・要検証)
- 理科年表基準: 太陽の伏角が **-7度21分40秒**(10進で約 -7.36111度)になる時刻を明六つ・暮六つとする。
- 一般的な市民薄明(civil twilight, -6度)とは異なるので要注意。
- `sunriset.c` のマクロ:
  ```c
  #define civil_twilight(year,month,day,lon,lat,start,end) \
      __sunriset__( year, month, day, lon, lat, -6.0, 0, start, end )
  ```
  この `-6.0` を `-7.36111` に変えた自前のマクロ/関数を用意すればよい。
- **要確認事項**: -7°21'40" という値の出典(理科年表)の再確認、あるいは他の資料との整合性チェック。角度の解釈(太陽中心か上端かなど `upper_limb` 引数の扱い)も要確認。

### 昼夜・十二支区分の計算
```
昼の長さ = 暮六つ時刻 - 明六つ時刻
夜の長さ = 24時間 - 昼の長さ

昼側十二支(卯・辰・巳・午・未・申、6区分) = 昼の長さ / 6
夜側十二支(酉・戌・亥・子・丑・寅、6区分) = 夜の長さ / 6
```
- 緯度経度は既存の `sunrise_sunset_face` と同じ入力方法を流用(ハードコード or 設定画面)。
- 毎日、日付が変わるタイミングで明六つ・暮六つを再計算する必要がある(季節で長さが変わるため)。

### 十二支ローマ字表記
```c
static const char *junishi_romaji[12] = {
    "NE", "USHI", "TORA", "U", "TATSU", "MI",
    "UMA", "HITSUJI", "SARU", "TORI", "INU", "I"
};
```

### 四分割の略称(3文字、表示エリアTOP=3文字に収まる想定)
```c
static const char *quarter_abbr[4] = {
    "HTO",  // 一つ (hitotsu)
    "FTA",  // 二つ (futatsu)
    "MTU",  // 三つ (mitsu)
    "YTU",  // 四つ (yotsu)
};
```
- 表示レイアウト案:
  - `WATCH_POSITION_TOP`(3文字) に quarter_abbr、`WATCH_POSITION_BOTTOM` に十二支名(USHIなど)を表示。
  - 「丑三つ時」の表示例: 上段 `MTU` / 下段 `USHI`
- **要確認**: カスタムLCDの各桁位置(特にposition 0/1)で表示可能な文字セットに制限があるかもしれない。`character_set_face` を実機で表示させて H/T/O/F/A/M/U/Y/I 等が問題なく出るか確認しておくこと。

### 実装ステップ案
1. `sunrise_sunset_face.c` を参考に、緯度経度取得〜`__sunriset__`呼び出しの部分を流用
2. 明六つ・暮六つを独自角度(-7.36111度)で計算する関数を追加
3. 現在時刻が昼夜どちらに属するか判定
4. 該当する側の6区分のうち、どの十二支に該当するか計算
5. その区分内でのさらに4分割位置を計算
6. ローマ字名+四分割略称を表示

---

## 2. 旧暦(太陰太陽暦)

### コンセプト
- テーブル参照方式(多くの既存旧暦変換プログラムもこの方式を採用)。
- リアルタイムでの黄経計算は精度上の問題(朔が0時付近だと日付が1日ずれる等)があるため避ける。
- Flash容量(256KB)に対して、圧縮したテーブルなら数百バイト程度で収まる見込み。

### 精度に関する重要な注意
- **平均朔望月(29.530589日)を使った累積計算はNG**。1ヶ月ごとの誤差(約0.0006日)が100年で1日近くまで蓄積する。
- さらに、実際の朔望月は29.18〜29.93日の範囲で変動するため、平均値ベースの近似はそもそも実際の朔とズレる。
- **対策**: テーブルの各年のデータは、計算式で導出するのではなく、検証済みの天文暦(国立天文台暦計算室、koyomi8.com等)が示す実際の値を、年ごとに個別に書き写す。誤差が「蓄積」する構造そのものを作らない。

### テーブル構造(圧縮版)
```c
typedef struct {
    uint8_t  days_to_new_year;  // その年の1/1から旧正月(旧暦元日)までの日数
    uint8_t  leap_month;        // 0=閏月なし、1-12=この月の後に閏月が入る
    uint16_t month_lengths;     // 月の並び順で、ビットi=1なら30日(大)、0なら29日(小)
                                 // 平年12ビット、閏年13ビット使用
} lunar_year_t;

static const lunar_year_t lunar_table[/* 年数 */] = {
    // 年ごとに、検証済みの外部データから値を書き写して埋める
};
```
- サイズ目安: 4バイト/年 × 100年 = 400バイト
- 対象年数(何年から何年まで)は要検討。100年分でFlash容量には全く問題ない。

### 閏月処理のロジック
- 閏月は「3月の後」等の固定位置ではない。中気(二十四節気の偶数番目、12個)を含まない月が閏月になる、という不定期のルール。
- テーブル生成時にこのルールに基づく正しい `leap_month` 値を外部データから取得して書き写す(Sensor Watch側で中気計算をする必要はない)。
- 月番号を進める処理は、`leap_month`と一致する回だけ月番号を据え置いて閏フラグを立てる形にする:
```c
uint8_t month_number = 1;
bool is_leap = false;
uint16_t remaining_days = days_since_new_year;

for (uint8_t i = 0; i < 13; i++) {
    uint8_t month_len = (year_data->month_lengths & (1 << i)) ? 30 : 29;
    if (remaining_days < month_len) break;
    remaining_days -= month_len;

    if (month_number == year_data->leap_month && !is_leap) {
        is_leap = true;   // 月番号を進めず、同じ月をもう一周
    } else {
        month_number++;
        is_leap = false;
    }
}
```

### 年またぎの処理
- 1/1〜旧正月前日までは前年の旧暦12月が継続している状態なので、この期間は前年のテーブルの最後の月を参照する分岐が必要。実装時に見落としやすいので注意。

### テーブルデータの作成方法(要作業)
- ゼロから天文計算するのではなく、外部の検証済みソース(国立天文台暦計算室、koyomi8.com等)から対象年数分の「旧正月の新暦日付」「各月の大小」「閏月の位置」を取得し、Cの配列としてオフラインでスクリプト化して生成する。
- 既存コード `wareki_face.c`(和暦)が類似の「切り替わり日をテーブルで持つ」設計をしている可能性があるので、参考にできるか確認する価値あり。

### 表示案
- 月・日の数字表示に加え、閏月であることを示す表示(例: "URU"のような略称、または専用インジケーター)を検討。

---

## 未確定・要検討事項まとめ
1. 薄明角度 -7°21'40" の出典の再確認、`upper_limb`引数の扱い
2. カスタムLCDでの利用可能文字セットの実機確認(character_set_face)
3. 旧暦テーブルの対象年数レンジの決定
4. 旧暦テーブルデータの具体的な生成方法(どの外部ソースを使うか)
5. `wareki_face.c` の実装パターンが流用できるかの確認
